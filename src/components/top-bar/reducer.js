
const initialState = {
  searchText: '',
  itemsFound: [],
  searching: false
};

export default function(state = initialState, action) {
  switch (action.type) {

    default:
      return state;
  }
}
